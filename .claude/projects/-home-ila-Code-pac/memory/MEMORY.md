# PAC Extension Project Memory

## Build
- Use `GEN=ninja make` for builds (user preference)
- Makefile includes `extension-ci-tools/makefiles/duckdb_extension.Makefile`
- Extension name: `pac`

## Project Structure
- DuckDB extension project
- Submodules: `duckdb`, `extension-ci-tools`, `benchmark/sqlstorm/SQLStorm`
