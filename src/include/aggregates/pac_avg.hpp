// pac_avg has been removed. avg() is now rewritten to pac_sum(hash,col)/count(col) by the PAC compiler.
// pac_avg_distinct (DISTINCT avg) is still supported via pac_distinct.hpp.
#ifndef PAC_AVG_HPP
#define PAC_AVG_HPP
#endif // PAC_AVG_HPP
